# Add a rule to /etc/security/group.conf that adds users to the "input" group, which is required
# for fusuma
echo 'w /etc/security/group.conf - - - - "*;*;*;;input"' > /usr/lib/tmpfiles.d/input-group.conf
